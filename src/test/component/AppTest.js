var assert = require('chai').assert;
var App = require('../../component/App');

describe('Progressadd', () => {
  it('progressbar', () => {
    // fetch("http://pb-api.herokuapp.com/bars",
    //       { method: 'GET',
    //         headers: {"Content-Type" : "application/json"}
    //       }).then(res => {
    //         if(res.bars
    //       })
    
  })
})
